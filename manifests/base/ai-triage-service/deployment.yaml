apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-triage-service
  namespace: service
  labels:
    app: ai-triage-service
    app.kubernetes.io/name: ai-triage-service
    app.kubernetes.io/part-of: ai-care
    app.kubernetes.io/component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-triage-service
  template:
    metadata:
      labels:
        app: ai-triage-service
        app.kubernetes.io/name: ai-triage-service
        app.kubernetes.io/part-of: ai-care
        app.kubernetes.io/component: backend
    spec:
      serviceAccountName: backend
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: ai-triage-service
        image: 105959916837.dkr.ecr.ap-northeast-2.amazonaws.com/ai-triage-service:latest # 나중에 치환될 부분
        ports:
        - containerPort: 8080
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 768Mi
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 20
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        envFrom:
        - configMapRef:
            name: ai-triage-service-config
        - secretRef:
            name: ai-triage-service-secret
      volumes:
      - name: tmp
        emptyDir: {}
