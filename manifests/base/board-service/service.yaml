apiVersion: v1
kind: Service
metadata:
  name: board-service
spec:
  selector:
    app: board-service
  ports:
    - protocol: TCP
      port: 8080          # 서비스가 노출할 포트
      targetPort: 8080    # 파드 컨테이너 내부의 포트 (containerPort와 일치)
  type: ClusterIP